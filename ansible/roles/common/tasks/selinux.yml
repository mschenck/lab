---

- name: install libselinux-python
  tags: common
  yum:
    name: libselinux-python
    state: present

- name: set SELinux state
  tags: common
  selinux:
    policy: "{{ selinux_policy }}"
    state: "{{ selinux_state }}"
