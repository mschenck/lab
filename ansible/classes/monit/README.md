monit - parameterized ansible role
=====================================

Params
------

* name (**required**) - name of monitoring target
* pidfile (**required**) - pidfile to monit
* check\_type (optional) - defaults to "process"
